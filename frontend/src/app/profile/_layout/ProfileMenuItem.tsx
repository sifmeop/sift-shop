interface ProfileMenuItemProps {
  name: string
  href: string
  icon: string
}

export const ProfileMenuItem = ({ name, href, icon }: ProfileMenuItemProps) => {
  return <div>ProfileMenuItem</div>
}
